<%= form_for(@to_do_list) do |f| %>
  <% if @to_do_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@to_do_list.errors.count, "error") %> prohibited this to_do_list from being saved:</h2>

      <ul>
      <% @to_do_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :list_name %><br>
    <%= f.text_field :list_name %>
  </div>
  <div class="field">
    <%= f.label :list_item %><br>
    <%= f.text_field :list_item %>
  </div>
  <div class="field">
    <%= f.label :item_completed %><br>
    <%= f.check_box :item_completed %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
